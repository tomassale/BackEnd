<% if(products.length !== 0) { %>
<div class="table-responsive">
  <table class="table table-dark">
    <tr style="color: yellow">
      <th>Nombre</th>
      <th>Precio</th>
      <th>Stock</th>
      <th>URL Imagen</th>
    </tr>
    <% for (const producto in products ) { %>
    <tr>
      <td><%= products[producto].nombre %></td>
      <td><%= products[producto].precio %></td>
      <td><%= products[producto].stock %></td>
      <td>
        <img width="10%" src="<%= products[producto].imagen %>"/>
      </td>
    </tr>
    <% } %>
  </table>
</div>
<% } else{ %>
<h3 style="color: black">no existen datos</h3>
<% }%>
